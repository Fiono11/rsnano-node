[package]
name = "rep-weights-converter"
version = "0.1.0"
edition = "2024"

[dependencies]
rsnano_ledger = { path = "../../ledger" }
rsnano_types = { path = "../../types" }

[features]
rai_protocol = ["rsnano_ledger/rai_protocol", "rsnano_types/rai_protocol"]
